name: Build and Deploy Astro

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: Checkout repo
        uses: actions/checkout@v4

      # 2️⃣ Устанавливаем Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 3️⃣ Устанавливаем Astro глобально
      - name: Install Astro globally
        run: npm install -g astro

      # 4️⃣ Устанавливаем зависимости проекта
      - name: Install dependencies
        run: npm install

      # 5️⃣ Сборка проекта
      - name: Build Astro site
        run: astro build

      # 6️⃣ Загружаем dist как артефакт
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: ./dist

      # 7️⃣ Деплой на GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages